# Clean Sweep

Clean Sweep is a Python utility that organizes the contents of a selected folder. It moves text files, CSV files, and folders into categorized subdirectories, while removing directories whose names contain `temp`.
